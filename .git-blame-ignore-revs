# Revisions to skip when running `git blame`.
#
# Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub applies this file automatically. Only add commits that are purely
# mechanical — a formatter run, a rename — never one that changes behaviour.

# Placeholder: replaced with the workspace-wide Prettier reformat hash below.
