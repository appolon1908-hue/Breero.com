# Final auth exact-head CI

This ordinary connector commit follows the async-auth regression cleanup so required backend workflows evaluate the final Argon2 migration, legacy PBKDF2 upgrade, strict local JWT verification, shared JWKS client, lifespan refresher, and deterministic tests on the exact review head.
